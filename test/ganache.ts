import {MockProvider} from "ethereum-waffle"

describe('Ganache', () => {
  let provider: MockProvider

  beforeEach(() => {
    provider = new MockProvider()
  })

  it('works', async () => {

  })
})
